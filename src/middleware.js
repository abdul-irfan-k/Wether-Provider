// import { connectDB } from "../DbConfig/Dbconfig";

const Middleware = async (req) => {
    // console.log('req',req)
}

export default Middleware
export const config = {
    matcher: '/api/:path*'
};